{
  "name": "minimalist-lv",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build && pagefind --site dist && node -e \"const fs=require('fs'); try{fs.cpSync('dist/pagefind','public/pagefind',{recursive:true})}catch(e){}\"",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.4.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.3"
  }
}
